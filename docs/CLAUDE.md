# docs/

Committed prose and generated reports. Nothing here may contain game source; edits to game
classes are described in words.

| File | What |
|---|---|
| `override-edits.md` | the authoritative prose log of every change in `src/overrides/`; extend it whenever an override changes |
| `results.md` | every run since the 2026-09-24 fresh start, in order (the metrics themselves live in Grafana, `harness/grafana/`) |
| `archive/2026-09-24/` | everything measured before the fresh start: the old `results.md`, every `findings-*.md`, `baseline-findings.md`, `native-baseline-2026-09-18.md`, `parity-watch-2026-09-22.md` and the last `benchmark-progress.html/.json` of the retired `harness/dashboard.py`; the runs themselves are in `harness/archive/2026-09-24/runs/` (local, gitignored) |
| `plan-driving-frame-time.md` | 2026-09-18 plan and driving target (p99 ≤ 10 ms, p99.9 ≤ 16.7 ms, no frame > 33 ms, jitter ≤ 1 ms); §3.1 superseded by baking, §3.3/3.4 done |
| `plan-resource-use.md` | 2026-09-19 verdict on why the uncapped game idles (Zink swap stall; NVIDIA GL is GPU-bound) |
| `plan-game-load.md` | the load after "Continue" (LoadTrace, loadtime.py) |
| `plan-vulkan-renderer.md` | plan to move from OpenGL to Vulkan |
| `plan-zombie-multithread.md` | 2026-09-22: the zombie simulation on all cores after the Louisville pass — where the game thread's 31 ms go, the six hazards (static scratch, pools, global toggles, events, cross-entity reads, determinism), phases in order of gain per risk (LOS read pass, action-context evaluation, character render prep, lighting JNI reads per chunk, reductions), the shared batch / checksum rigs, ~32 → ~45 fps ceiling; sharding the zombie update itself is out |
| `plan-upscalers.md` | 2026-09-22: render-resolution upscaling — the composite seam (offscreen buffer, screen shader), the scaled world pass (`pzopt.RenderScale`: viewport + scissor on the render thread, IsoCamera's render-thread view, the screen-rect uniforms of water / puddles / fog / particles), the resolve (`pzopt.Upscaler`: bicubic / FSR 1.0 EASU + RCAS / DLSS through the Vulkan shim `src/native/pzopt_ngx.cpp`), modes and keys, the run log (120 km/h drive: off 509 → bicubic 676 / fsr1 619 fps at 50 %); 2026-09-23 second pass: the FSR second view cone fixed, the DLSS cost taken apart (evaluation / input copies / GL resume gap), the reduced-output + RCAS DLSS defaults (+22 % over off in a GPU-bound 4K scene), image quality per mode |
| `plan-graphics-enhancements.md` | 2026-09-25: modern graphics features that fit the renderer after AO — height-field sun shadows with soft penumbra (first pick), per-pixel lighting in four phases (blend, normals, unlit bake + per-frame light composite that ends lighting re-bakes, shadowed dynamic lights), foliage wind, water/puddle reflections, light shafts, cloud shadows, SMAA/DoF; second pass (deep research, same day): player demand ranked (the void, blur, black rooms, flat look, headlights, zoom, night tint), 15 new candidates A-O (sprite filtering, darkness floor + memory tint + LUT grading, dynamic resolution from the GPU timer, ambient-cube + capsule shadows for characters, local-light contact shadows, height-field decals, one world-aligned snow / ripple / wetness texture, height-field particles, froxel fog, GTAO multi-bounce + SSGI, water details, emissive tiles, offline material classes), pitfalls (double shading), skip list (frame generation ...), revised order |
| `plan-wayland.md` | 2026-09-19 native Wayland vs XWayland A/B: a wash at the cap, XWayland stays default |
| `findings-scene-presets-2026-09-20.md` | the night / torch / thunderstorm presets: night and torch free, storm 83 fps with p99 43 ms and nothing saturated (chunk lighting rebakes ×5); capture rig blind to the player light; next steps |
| `findings-fog-2026-09-21.md` | heavy fog at no cost: where stock `ImprovedFog` spent 1.5 ms of GPU and a game-thread square walk, the fog pass (one draw call, scaled fog buffer, in-place scene depth, min-depth reduction + depth-aware composite, mipmapped noise, chunk fog masks + segment cache), laptop and desktop run tables, what is left |
| `findings-blocky-lights-2026-09-21.md` | the maintainer's "blocky lights" video: a moving torch / headlight beam through the held lighting-only re-bakes and the lighting-refresh queue (dirty bits lost past a pass); `pzopt.LightDirt` (strong changes re-bake now, global-light events keep the spread) + the pre-pass queue flush; run table with the consecutive-frame hard-jump metric |
| `findings-world-sound-2026-09-22.md` | the `WorldSoundManager.addSound` hitch: the fish-scaring square walk and the `(2r/8)²` chunk walk per call, quadratic in the radius, per frame while a house alarm rings (0.4 ms at 600, 4.4 ms at 2000); `worldSoundFast` (chunk walk clamped to the loaded grid, identical-call memo in `addSoundNoise`) → 0.007 ms; rig `--flag sound=R` |
| `findings-characters-draw-2026-09-22.md` | the characters draw (game-thread render sub-phase) on the Louisville horde: what stock spent there (initModel per zombie, one executor submit per zombie — `init` was already threaded —, the shadow ellipse of every culled zombie, the HashSet walk), `charDrawPrep` (pzopt.CharDraw: the object walk on a worker, the draw data of the model zombies built on the slot-init executor during the chunk bakes, the stock loop over the on-screen list) and `zombieAtlasFast` (the culled zombies' flat draw), run table `cd*-*`, 13 → under 5 % |
| `findings-darkness-grading-2026-09-26.md` | candidate B of the graphics plan: the darkness floor on the native per-square light (seen squares, basements exempt; no frame cost), remembered places drawn by the vision-cone pass itself (desaturated / dimmed / cooled out-of-sight areas, remembered rooms kept; cheaper than the stock pass), colour grading by time of day and weather (Purkinje night, golden hour, rain, storm, fog, snow; `.cube` overlays) fused with the stock screen shader's tail in one 65³ LUT (the composite ~29-37 us a frame cheaper than stock at 5K); the cost runs `dk-*` (same-run on / off GPU sections), the ablations, the mapped-buffer uploads |
| `findings-updater-2026-09-26.md` | the near-instant updater: what changes between releases (3 of 613 zip entries), the delta download from the zip's central directory by range requests, background prefetch, ETag check during boot, Workshop copy (issue #16), Restart game; every idea's measurement (kept and dropped: HTTP/3, hard links, zstd, stat cache) on the desktop and the Mac |
| `findings-sound-2026-09-24.md` | the sound engine pass: what FMOD and the game thread spend on sound in a horde + storm + alarms + gunfire + helicopter scene, the audio defects Jev found (clipping in the OS downmix of the game's fixed 5.1 / 32 kHz mix, the quit cutting a thunder, a rain gap) and the fixes (`emitterIdleSkip`, `soundTickHz`, `audioLimiter` + stereo fold, the harness exit fade), the audio judge's calibration |
| `plan-diagram.md` | plan at a glance |
| `proton-run-prep-2026-09-19.md` | command sequence for the pending Proton comparison |
| `workshop.md` | the Steam Workshop item: layout, validator rules, upload steps (Steamworks API, `scripts/workshop-upload.py`); `workshop/description.txt` is the page body (BBCode, placeholders), `workshop/workshop.txt` keeps the item id after the first upload |
| `windows-test.md` | installing the prebuilt class zip on a Windows depot and testing it; 2026-09-19 results of the first Windows bench (stock vs optimized, utilization) |
| `recalc-static-audit.md` | static-state audit of the recalc pass (tools/StaticAudit) |
| `media-style.md` | the media style (dark surface, the four colour roles in HDR PQ and SDR values, Noto Sans / Mono, layout, encoding); every new video, poster or README image follows it |
| `media/drive-results.svg` | generated by `harness/readme-chart.py` |
| `media/blocky-lights-before-vs-after.mp4` (+ `.jpg` poster) | `harness/stitch-blocky-lights.sh`: three 20 s segments, each BEFORE / AFTER / STOCK — the night-torch spin at zoom 1, the 120 km/h SportsCar night drive, the ambulance (headlights + lightbar) night drive; AV1 HDR 3840x790, 60 s (2026-09-21, the "blocky lights" fix) |
| `media/bench-e400-storm-helicopter-stock-vs-fixed.mp4` (+ `.jpg` poster) | `harness/stitch-heli-storm-sbs.sh`: the E:400 teleport bench in a thunderstorm with the stock helicopter event over the route, stock `addSound` path (left) vs `worldSoundFast` (right), aligned at the teleport onset, overlay insets; AV1 HDR 3840x810, 27 s (2026-09-22, runs `heli-storm-e400-*`) |
| `media/bench-storm-house-alarm-stock-vs-fixed.mp4` (+ `.jpg` poster) | `harness/stitch-alarm-storm-sbs.sh`: the thunderstorm spin route with a ringing house alarm (600-radius world sound every frame), stock `addSound` path (left, 242 fps, 16 frames > 33 ms) vs `worldSoundFast` (right, 290 fps, 2), overlay insets; AV1 HDR 3840x810, 30 s (2026-09-22, runs `alarm-storm-rec-*`); the video of the world-sound fix |
| `media/drive-120-south-two-runs.mp4` (+ `.jpg` poster; the committed README copy is `-1080.mp4` / `-1080.jpg`, CQ 30) | `harness/stitch-drive-pair.sh dsv-1 dsv-2`: two recorded runs of the `drive-120-south` path drive (KY-60 east, south through Rosewood, 120 km/h, 240 cap) side by side, aligned on the route start, each panel with the path pilot's live readout; results strip with both runs' frame numbers, drive cards and Jev verdicts (valid 1.00 / 1.00, consistent 0.84: 0.6 km/h / 0.13 tiles / 0.02 s apart). AV1 HDR 5120x1910, 41 s (2026-09-24) |
| `media/bench-zoom-spin-stock-vs-new.mp4` (+ `.jpg` poster; the committed README copy is `-1080.mp4` / `-1080.jpg`, CQ 30) | `harness/stitch-zoom-sbs.sh`: the south bench route with a 0.25 ↔ 2.5 camera zoom spin every 3 s, the stock zoom paths on our build (left: `zoomRetain=false zoomEaseMs=0`, textures freed off screen, 0.03-per-frame step and snap, 469 fps, 6 frames > 33 ms) vs `zoomRetain` + the 300 ms Bézier ease (right, 520 fps, 3), aligned at the start of the first zoom-out motion, overlay insets; AV1 HDR 3840x810, 30 s (2026-09-22, runs `zoom-rec-*`); the video of the zoom pass |
| `media/bench-storm-house-alarm-stock-vs-optimized-vs-fixed.mp4` (+ `.jpg` poster) | `harness/stitch-alarm-storm-triple.sh`: the same alarm storm route three ways in the 2:1 quad layout — stock game (38 fps, 178 frames > 33 ms), all optimizations with `worldSoundFast` off (242 fps, 16), all optimizations (290 fps, 2), results panel; AV1 HDR 3840x1920, 29 s (2026-09-22, runs `alarm-storm-rec-*`) |
| `media/louisville-horde-stock-vs-optimized-vs-player-los.mp4` (+ `.jpg` poster) | `harness/stitch-louisville-triple.sh`: the Louisville horde preset three ways in the 2:1 quad layout — stock game (21.3 fps, 417 frames > 33 ms), all optimizations with the player LOS pass off (31.0 fps, 316, player 14 % of the game thread), all optimizations (38.0 fps, 163, player 3 %), results panel; aligned at the quit-to-black, overlay insets (none on the stock panel), the results read from `analyze.py`; AV1 HDR 3840x1920, 26 s (2026-09-22, runs `losvid-*`, the worktree build HEAD + the player pass); the video of the player LOS pass |
| `media/louisville-horde-profiled-stock-vs-zombie-game-thread.mp4` (+ `.jpg` poster; the mp4 stays local) | `harness/stitch-duo.sh tri-lou-stock tri-lou-zt`: the Louisville horde, stock game (every key off, overlay + profiler on; 25.0 fps, p99 99.1 ms, 382 frames > 33 ms) beside the working tree with the zombie game-thread pass (62.4 fps, p99 36.2 ms, 23), both at half size so the overlay tree and flame graph stay readable, results strip underneath; AV1 HDR 5120x1950, 27 s (2026-09-22, runs `tri-lou-stock-20260922-105001`, `tri-lou-zt-20260922-191504`) |
| `media/zombie-thump-offscreen-before-vs-after.mp4` (+ `.jpg` poster) | `harness/stitch-thump.py thump240c-before thump240c-after`: three zombies thump a locked door, the player walks 35 tiles away (zombies scene-culled, SIXTEENTH sim level) and comes back; left = the bug restored (`devActionEvalUnitMultiplier=true`: 29 strikes in one second off screen, door -87 HP, then silence), right = the fix (a steady 1-3 strikes/s); per-second counters from the ThumpRig lines, 240 fps; AV1 HDR 3840x1382, 36 s (2026-09-22 night) |
| `media/overlay-responsive-4-screen-sizes.mp4` (+ `.jpg` poster) | `harness/stitch-overlay-sizes.sh`: the performance overlay fitted to 5120x2160 (half size), 1920x1080, 1280x720 and 1024x768 (1:1) on the Rosewood spin route — `overlayFont=auto`, the flame column's third-of-the-width reserve, graphs giving way for height, cut lines ending in `...`; AV1 HDR 5120x2600, 26 s (2026-09-22) |
| `media/vehicle-spawn-before-vs-after.mp4` (+ `.jpg` poster) | `harness/stitch-vehspawn.sh`: the never-visited Louisville airport lot on car spawn HIGH, released `IsoChunk.AddVehicles_OnZone` (left, 2 vehicles loaded: one row of stalls per chunk, High = a flat 2 %) vs the decompiler fix (right, 68); AV1 HDR 5120x1730, 24 s (2026-09-23, runs `vehspawn-*`, the Workshop "cars barely spawn" reports) |
| `media/darkness-grading-stock-vs-new.mp4` (+ `.jpg` poster; stays local until published) | `harness/stitch-darkness.py` (queued media job): candidate B, four scenes of the Rosewood house with the player turning in place, the stock look (left) beside the new options (right) from matched runs captured in-game (`devCapture=15.5,9,30,75`, runs `cap-*-off` / `cap-*-on`; the desktop recorder had a browser window over the game), aligned on the route start: night (floor 20 % + remembered places + grading), clear day (remembered places), golden hour and rain (grading), then the cost card (runs `dk-cost-clean`, `dk-ab-*`); AV1 HDR 3840x1800, 40 s (2026-09-26) |
| `media/workshop-mods-comparison.png` | generated by `harness/mods-table.py` (the Workshop mods table in the README; numbers typed in from results.md) |
| `media/dell-lowend-comparison.png` | generated by `harness/lowend-table.py` (the Workshop page's "Low-end hardware mode" image: stock vs profile on the Dell, dated; numbers typed in from results.md) |
| `media/updater-card.png` (+ the four `media/updater-*.png` captures) | generated by `harness/updater-card.py` (the Workshop page's "New! In-game updater" image: the menu item greyed / enabled, the dialog before and after the install; 2026-09-22) |
| `media/mac-comparison.png` | generated by `harness/mac-table.py` (the Workshop page's "New! macOS support" image: stock vs optimized on the M1 Pro MacBook, 120 km/h drive, two-run means, dated; numbers typed in from results.md) |
| `media/upscaler-card.png` | generated by `harness/upscaler-card.py` (the Workshop page's "New! Upscaler: FSR 1.0 and DLSS" image: the 120 km/h drive per upscaler mode, fps / p50 / p99, dated; numbers typed in from plan-upscalers.md; 2026-09-22) |
| `media/hdr-card.png` | generated by `harness/hdr-card.py` (the Workshop page's "New! HDR output (Linux, macOS)" image: stock SDR vs HDR at the defaults, panel nits per scene from the comparison video, frame cost; 2026-09-24) |
| `media/hdr-stock-vs-hdr-vs-enhanced.mp4` (+ `.jpg` poster; the committed README copy is `-1080.mp4` / `-1080.jpg`, CQ 30; the full-size mp4 stays local) | `tools/hdr/stitch-hdr-3way.sh` (queue media job, `LAG_<scene>_<pane>` from frame correlation): stock (`enabled=false`, SDR) / HDR with the plain highlight expansion only (`tools/hdr/tune/plain.txt`) / HDR enhanced (defaults) + legend, 2x2 per scene: night torch + lamps + fires, river shore by day, thunderstorm; the character turns in place; AV1 HDR 5120x2160, 46 s (2026-09-24, runs `hdrvid-*`) |
| `media/hdr-showcase-sdr-vs-hdr.mp4` (+ `.jpg`; committed copy `-1080.mp4` / `-1080.jpg`; full size local) and `media/hdr-night-fires-sdr-vs-hdr.mp4` (+ `.jpg`) | `tools/hdr/reel.sh` / `stitch-hdr-sbs.sh`: stock SDR vs HDR side by side, fires + torch, headlights, lightning (2026-09-24 morning, runs `hdrcmp-*`) |

Conventions: date findings in the heading, put numbers in tables, name the runs they came
from (label + timestamp under `harness/runs/`), and note resolution / renderer / zoom /
launcher so the run is comparable later. Start new work from the relevant plan's open
sections rather than re-deriving.
