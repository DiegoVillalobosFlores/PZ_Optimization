#version 330
// pzopt: placeholder. pzopt.PixelLight.patchShader supplies the real source of this program (the per-pixel light
// base variant of the chunk composite) when the game compiles it; nothing else uses these files.
void main() {}
